"""
docpler - Document processing tool.
HWP, and more formats to Markdown.

DEPRECATED: docpler has been renamed to hwpcli.
Install the successor with:  pip install hwpcli
"""
import warnings

warnings.warn(
    "docpler has been renamed to hwpcli. Install the successor with "
    "`pip install hwpcli` and import from `hwpcli.hwp` instead. "
    "This package will not receive further updates.",
    DeprecationWarning,
    stacklevel=2,
)
